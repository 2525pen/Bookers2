<div class="container">
	<div class="row">
		<div class="col-xs-4">
			<%= render 'books/profile' , user: @user %>
			<%= render 'users/posts', book: @book %>
		</div>
		<div class="col-xs-8">
			<table>
					<thead>
						<tr>
							<th>image</th>
							<th>title</th>
							<th>Opinion</th>
						</rt>
					</thead>

					<tbody>
							<tr>
								<% @users_books.each do |book| %>
									<td>
										<%= attachment_image_tag @user, :profile_image, :fill, 100, 100, fallback: "no_image.png", size: '40x40' %>
									</td>
									<td>
										<%= link_to book.title, book_path(book) %>
									</td>
									<td>
										<%= book.body %>
									</td>
								<% end %>
							</tr>
					</tbody>
			</table>
		</div>
	</div>
</div>